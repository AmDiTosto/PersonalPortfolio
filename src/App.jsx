import { useState } from "react";

function App() {
  const [count, setCount] = useState(0);

  return (
    <div class="w-full h-full bg-gray-500">
      <h1>Hello world</h1>
    </div>
  );
}

export default App;
